# Leave empty for local dev — Vite's dev server proxies /api, /ws, /storage
# to the backend on localhost:8000 (see vite.config.js).
#
# In production, set this to the deployed backend's origin (no trailing
# slash), e.g. https://clipninja-backend.onrender.com
VITE_API_BASE_URL=
